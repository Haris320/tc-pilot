# Frontend env — the only key the frontend ever sees is the FastAPI base URL.
# All sponsor secrets (Anthropic, ClickHouse, Nimble, Datadog) live in backend/.env only.
NEXT_PUBLIC_API_URL=http://localhost:8000
